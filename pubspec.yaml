name: rubcon
description: Rubcon project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_swiper: ^1.1.6
  http: ^0.13.4
  provider: ^6.0.2
  animations: ^2.0.2
  intl: ^0.17.0
  url_launcher: ^6.0.20
  dio: ^4.0.6
  stomp_dart_client: ^0.4.4
  flutter_local_notifications: ^9.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/HelloLayer.png
    - assets/rubconImage.png
    - assets/primaryBg.png